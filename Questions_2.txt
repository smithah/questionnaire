Programming
1) Closest numbers linked list.
2) Minimum moves to equal array elements. 
3) Reverse a linked list.
4) Suspicious log activity. 
5) Error in the code of Linked list.

1. How many types of Streams are there in Java.io
2. Types of Parser in XML.
3. Why Thread Pool is required.
4. Why there are two types of streams in Java
5. H.323 Protocol
6. JNDI
7. Difference between SS7 and SIP
8. Serialization and Deserialization of JSON
9. What is DOM in XML
10. Types of Parsers in XML
11. Suppose there are 5 deployments of WAR file in load balancer in a publish subscriber model.And if a published message should be processed by only one deployment
then how to prevent it from being processed by other deployment.
12. Have you used filters in Servlets.


1. What is factory pattern
2. What will happen if we insert two same key 1 - Smitha and 1- Sheshari to HashMap
3. Difference between HashMap and Concurrent HashMap
4. What is serialization in Java
5. Why is there a functional interface in Java 1.8
6. Features of Java 1.8
7. What is streams in Java
8. What are the Databases worked one. Apart from CRUD operation have you written 
Stored Procedures.
9. How to create a custom Exception Class and how to use it in your code.
10. What is the finally block required for.

10. Name a data structure which maintains the insertion order.
11. Do you know the internal implementation of Hashmap.

1. Print the Highest and Lowest Number in a list of lists 
Example: //{{1,3,5,-9,-7},{2,-2,-8,10},{13,14,99,200,-27}}

2. Supposedly three threads are running to format a file parallelly and independently
how would you program such that a fourth thread should start off only after the  first
three threads have finished their work.

3. What data structure to use such that the numbers being inserted should always be in a 
sorted order.(Say for Example numbers should always sit in the sorted manner on insertion)
without any programming.

4. If there are two synchronized methods one is a method A static synchronized another method B an instance normal method which is synchronized. If a thread is accessing a method A then can another thread access method B ?

1. Print the Highest and Lowest Number in a list of lists 
Example: //{{1,3,5,-9,-7},{2,-2,-8,10},{13,14,99,200,-27}} -- Explain the time complexity or space complexity.

5. multi-threading- object vs class level locking.



4. If there are two synchronized methods one is a method A static synchronized another method B an instance normal method which is synchronized. If a thread is accessing a method A then can another thread access method B ?


